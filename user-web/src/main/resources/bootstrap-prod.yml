spring:
  cloud:
    config:
      name: jaagro-config-user-9020-client     #需要从github上读取的资源名称，注意没有yml后缀名
      profile: prod
      label: master
      uri: http://47.97.193.119:9040     #SpringCloudConfig获取的服务地址

  # 日志输出
  profiles:
    active: dev